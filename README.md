# HOMS-GUI

This repository now includes a simple, static landing page inspired by the 18Birdies website. Open `index.html` directly in your browser or run a quick local server to view it:

```bash
python -m http.server 8000
```

Then navigate to `http://localhost:8000` to explore the experience.
